#include <stdio.h>
#include <string.h>

int main()
{
     // char str[100];
     // gets(str);
     // strlwr(str);
     // int i,a, j;
    
     // for (i=0; i<strlen(str); i++)
     // {
          // if (i==0) printf("%c", str[0]);
          // else{
               // if (str[i-1]==32 && str[i] != 32){
                    // printf("%c", str[i]);
                    // a=i+1;
               // }
          // }
     // }
     
     // for (int j=a; j<strlen(str); j++){
          // printf("%c", str[j]);
     // }
     // printf("@ptit.edu.vn");
     char c=' ';
     printf("%d",c);
}